import { app } from "./app";
import { shell } from "./shell";
import { theme } from "./theme";
import { window } from "./window";
import { db } from "./db";

export const router = {
  theme,
  window,
  app,
  shell,
  db,
};
